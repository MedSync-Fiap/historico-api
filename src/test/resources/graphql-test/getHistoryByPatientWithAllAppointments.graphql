query GetHistoryByPatient($patientId: ID!) {
    getMedicalHistoryByPatientId(patientId: $patientId) {
        patient {
            id
            name
        }
        appointments(filter: {onlyFuture: false}) {
            id
        }
    }
}
