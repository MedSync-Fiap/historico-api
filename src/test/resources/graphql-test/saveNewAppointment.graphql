mutation SaveNewAppointment($input: AppointmentInput!) {
    saveNewAppointment(newAppointmentInput: $input) {
        patient {
            id
        }
        appointments {
            id
        }
    }
}