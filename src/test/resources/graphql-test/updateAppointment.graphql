mutation UpdateAppointment($input: AppointmentInput!) {
    updateAppointment(updateAppointmentInput: $input) {
        patient {
            id
        }
        appointments {
            id
        }
    }
}
